package com.example.bbzn;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

@RunWith(SpringRunner.class)
@SpringBootTest
public class BbznApplicationTests {

    @Test
    public void contextLoads() throws Exception {
        /*PushWork pushWork = new PushWork();
        pushWork.setAppkey("2a35d9414a2fd");
        pushWork.setPlats(new Integer[1]);
        pushWork.setTarget(1);
        pushWork.setType(1);
        pushWork.setContent("测试内容");
        pushWork.setAndroidTitle("测试");
        PushClient pushClient = new PushClient();
        String results = pushClient.sendPush(pushWork);
        System.out.println(results);*/
    }

}
